<Window x:Class="AutoGameHDR.GameListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Lang_UserListTitle}" Height="450" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource PrimaryText}"
        Style="{StaticResource {x:Type Window}}"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="{DynamicResource Lang_UserListMainHeader}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBlock Text="{DynamicResource Lang_UserListDesc}" Foreground="{DynamicResource SecondaryText}" FontSize="12"/>
        </StackPanel>

        <DataGrid x:Name="GameGrid" Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserResizeRows="False"
                  HeadersVisibility="Column" 
                  BorderThickness="1" 
                  RowHeight="36"
                  Background="{DynamicResource WindowBackground}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                  VerticalGridLinesBrush="Transparent">

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="{DynamicResource Lang_ColEnabled}" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="{DynamicResource Lang_ColProcessName}" Binding="{Binding ProcessName}" Width="*" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="10,0,0,0"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="{DynamicResource Lang_ColAction}" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{DynamicResource Lang_BtnDelete}" Click="DeleteButton_Click" 
                                    Height="26" Width="60"
                                    FontSize="12"
                                    Padding="0"
                                    BorderThickness="1"
                                    Background="Transparent"
                                    BorderBrush="#FFCDD2"
                                    Foreground="#E53935"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#FFEBEE"/>
                                                <Setter Property="BorderBrush" Value="#E53935"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="{DynamicResource Lang_BtnImport}" Click="Import_Click" 
                        Height="36" Padding="15,0" Margin="0,0,10,0"
                        Background="Transparent" BorderThickness="1" 
                        BorderBrush="{DynamicResource BorderBrush}" 
                        Foreground="{DynamicResource PrimaryText}"/>

                <Button Content="{DynamicResource Lang_BtnExport}" Click="Export_Click" 
                        Height="36" Padding="15,0" 
                        Background="Transparent" BorderThickness="1" 
                        BorderBrush="{DynamicResource BorderBrush}" 
                        Foreground="{DynamicResource PrimaryText}"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="{DynamicResource Lang_BtnSave}" Click="SaveButton_Click" 
                        Height="36" Padding="25,0" 
                        Background="#0078D7" 
                        Foreground="White" 
                        FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>